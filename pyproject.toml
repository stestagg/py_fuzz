[project]
name = "pyfuzz"
version = "0.1.0"
description = "Unified tooling for building, running, and analyzing py-fuzz projects"
requires-python = ">=3.11"
dependencies = [
  "click",
  "jinja2",
  "json-repair",
  "odhash"
]

[tool.setuptools.packages.find]
where = ["src"]
